version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - misspell
    - unconvert
    - prealloc
    - copyloopvar
    - gosec

  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
    revive:
      rules:
        - name: exported
          disabled: true
    gosec:
      excludes:
        - G104

  exclusions:
    paths:
      - internal/repository/generated
      - internal/app/wire_gen.go$
      - ".*_gen\\.go$"
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
      - path: cmd/server/main\.go
        linters:
          - gosec

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  fix: false
