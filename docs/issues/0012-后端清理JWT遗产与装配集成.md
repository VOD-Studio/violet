# Issue-0012：后端清理 JWT 遗产 + 装配 + 集成测试

## Parent

PRD-0002（`docs/prd/0002-登录态opaque-session重构.md`）

## What to build

删除全部 JWT / refresh / rotation 遗产（无兼容层），重写 auth_container 装配 opaque session 链路，补端到端集成测试。完成后后端认证彻底切换到 opaque session，可独立验证。

## Acceptance criteria

### 删除遗产
- [ ] 删 `infrastructure/auth/jwt.go` + `jwt_test.go` + `redis_store.go` + `redis_store_test.go`（refresh store）
- [ ] 删 RefreshTokenHandler + `auth_commands_test.go` 中 TestRefresh_* 四个测试
- [ ] ports.go 删 TokenService / TokenStore / TokenPair / TokenInput / Claims / RotateResult
- [ ] mocks.go 删 MockTokenService / MockTokenStore
- [ ] auth_adapters.go 删 TokenServiceAdapter
- [ ] config.go 删 JWTPrivateKeyPath / JWTPublicKeyPath / JWTAccessTokenTTL / JWTRefreshTokenTTL / JWTAllowEphemeralKey + 校验；config.example.yaml 同步
- [ ] 路由删 POST /auth/refresh，加 GET /auth/session

### 装配
- [ ] NewAuthContainer 重写：装配 SessionStore / CreateSessionHandler / 各 handler（无 token 依赖）；AuthContainer 用 SessionStore 取代 JWTService
- [ ] 中间件装配处：`middleware.SessionAuth(sessionStore, cfg.Cookie, cfg.Session.IdleTTL)` 取代 `Auth(jwtValidator, WithAccessCookie)`
- [ ] /auth/session 挂 OptionalSessionAuth 只读；受保护路由挂 SessionAuth；admin 路由挂 SessionAuth + AdminRequired

### 集成测试
- [ ] `auth_integration_test.go`（miniredis + httptest + 真实 handler 链）：
  - POST /auth/login → Set-Cookie mimo_session + mimo_csrf，body user_id
  - 用 cookie GET /auth/session → 200 claims，**mock 期待不调 Touch**（只读命门）
  - 用 cookie GET 受保护端点 → 200，session TTL 重置（调 Touch）
  - POST /auth/logout（带 cookie + CSRF header）→ 200 + Set-Cookie Max-Age=0
  - logout 后再 GET /auth/session → 401
  - PATCH /auth/password 成功后该用户另一 session 也失效（DeleteByUser）
- [ ] `go build ./...` 无错误；`go test ./...` 全绿

## Blocked by
- Issue-0010（login / logout 改造）
- Issue-0011（cookie 层 + /auth/session + 改密吊销）
