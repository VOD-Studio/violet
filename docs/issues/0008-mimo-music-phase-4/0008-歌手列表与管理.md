# Issue-0008：歌手列表浏览与收藏管理

## Parent

PRD：`../../prd/0008-mimo-music-phase-4.md`（user stories 44-46；Implementation Decisions - 歌手扩展）
前置：0007（ArtistService 已扩展）

## What to build

补充歌手的全局浏览和写操作接口：热门歌手列表、歌手分类列表、收藏/取消收藏歌手。

热门歌手和分类列表是全局浏览（非按 artistID 查），放 ArtistService 作为列表 rpc。收藏是写操作，需登录态。

proto `ArtistService` 加 rpc：

- **TopArtists**（热门歌手）：`TopArtistsRequest{offset, limit}`，响应 `repeated Artist` + 分页。网易云 `/weapi/artist/top`。`MapResponse` 调 `model.MapArtists`。
- **Categories**（歌手分类列表）：无参数，响应 `repeated ArtistCategory`。网易云 `/weapi/artist/list`（带 cat 参数为分类，不带为列表）。
- **Subscribe**（收藏/取消，toggle）：`SubscribeRequest{artist_id}`，响应 `{code, subscribed}`。网易云 `/weapi/artist/sub`。

TopArtists/Categories `CachePolicy` TTL 24h，`AuthAnonymous`。Subscribe `CachePolicy=nil`，`AuthLoggedIn`。

新建 proto message：`ArtistCategory`（id/name/alias/img）。

## Acceptance criteria

- [ ] `proto artist.proto` 的 `ArtistService` 加 3 个 rpc + 对应 message
- [ ] 3 个 endpoint 声明（`endpoint/artist/`）
- [ ] TopArtists/Categories TTL 24h、AuthAnonymous；Subscribe CachePolicy=nil、AuthLoggedIn
- [ ] TopArtists 的 MapResponse 调 model.MapArtists 复用
- [ ] service 3 个方法恒一行
- [ ] endpoint MapRequest/MapResponse 纯函数测试
- [ ] engine 集成测试：Subscribe 走登录态 cookie
- [ ] `make proto` 生成成功
- [ ] 所有导出符号有 godoc / proto 注释

## Blocked by

0007 —— 依赖 ArtistService 扩展的既定模式（proto 结构、endpoint 目录、MV/Video 实体定义）。
