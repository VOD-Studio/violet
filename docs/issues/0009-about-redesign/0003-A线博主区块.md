# Issue-0003：A 线区块（A1-A4 关于博主）

## Parent

PRD-0009（`../../prd/0009-about-redesign.md`）

## What to build

实现 A 线（关于博主）四个区块的真实组件，替换 Issue-0002 的占位。所有内容消费各自 settings 字段，接入骨架渲染。

- **A1 头像 + 标语**：圆形头像（`avatar_url`）+ 大字 tagline（`tagline`）
- **A2 名片卡**：role / location / available_for / email，精致卡片布局
- **A3 技能标签云（升级版）**：拆「擅长」「在学」「兴趣」三组，带配色（替换原 `tech_stack` 单字符串 split 的扁平呈现）
- **A4 社交矩阵**：扩展 GitHub / Twitter / Mastodon / Email / RSS / Bilibili 等（替换原只有 GitHub + 站点两卡的 SocialCard）

端到端切片：后端 settings 补齐 A 线所需字段（A3 的三组技能、A4 的各社交平台字段等，若 Issue-0002 未全落）→ 前台 A1-A4 区块组件实现 → 接入 `about_config` 骨架按配置渲染。

## Acceptance criteria

### 后端字段（settings）
- [ ] A1：`avatar_url`、`tagline` 已在 `SiteSettings` + `UpdateInput` + `GetPublic` 白名单（Issue-0002 落示例字段，此处补齐缺失的）
- [ ] A2：`role`、`location`、`available_for`、联系 email（可复用现有 `admin_email` 或新增公开邮箱字段）加入链路
- [ ] A3：技能三组字段（如 `skills_strong` / `skills_learning` / `skills_interests`，或一个 JSON 聚合字段）加入链路
- [ ] A4：各社交平台字段（`social_twitter` / `social_mastodon` / `social_email` / `social_rss` / `social_bilibili` 等，GitHub 复用现有 `github_username`）加入链路

### 前台区块组件（features/about/ui）
- [ ] A1：头像 + tagline 渲染，`avatar_url` 为空时优雅降级（不显示头像或占位）
- [ ] A2：名片卡布局，空字段不显示对应行
- [ ] A3：三组标签云分组渲染 + 配色，空组不渲染
- [ ] A4：社交矩阵，仅渲染已配置的平台（空平台不出现），图标 + 链接正确
- [ ] 各区块按 `about_config` 的 enabled/order/params 正确接入骨架（替换占位）

### 测试（Seam 1 延续）
- [ ] 各区块对空数据优雅降级（不渲染空字段 / 不报错）
- [ ] 各区块消费正确 settings 字段（给定字段值，断言渲染对应内容）

### 验收
- [ ] A1-A4 区块在前台正确渲染，空数据不报错
- [ ] `make api-test` / `make web-typecheck` / `make web-test` / 各 lint 全绿

## Blocked by

- Issue-0002（依赖 about_config 骨架与 settings 字段扩展基础）

---

## 完成状态：✅ 已完成

实现 commit（feat/about-redesign 分支）：
- 1c63140 feat(settings): 关于博主 A 线内容字段端到端
- b1d08a8 feat(about): A 线区块 A1-A4 关于博主组件

验收：typecheck / 522 测试全绿 / go vet 干净。
A1-A4 真实组件实现并注册，空数据优雅降级；后台 profile 子页可编辑全部 A 线字段。
