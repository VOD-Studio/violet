# Issue-0001：项目骨架与健康检查端点（tracer bullet）

## Parent

PRD：`../../prd/0005-mimo-music-phase-1.md`
架构：`../../adr/mimo-music-architecture.md`

## What to build

建立 mimo-music 项目骨架，打通从 go.mod → config → bootstrap → server → handler → response 的完整管道。用一个 `/health` 端点验证整条链路通：启动 HTTP 服务，访问 `GET /health` 返回 `{"code":0,"data":{"status":"ok"},"message":""}`。

这是 tracer bullet：只做骨架，不做任何业务逻辑。确立后续所有切片要复用的目录结构、响应信封、启动装配模式。

go.mod module path 为 `github.com/VOD-Studio/mimo-music`，Go 1.25，与 mimo-blog 一致。

## Acceptance criteria

- [ ] 新建 `mimo-music/go.mod`，module path `github.com/VOD-Studio/mimo-music`，go 1.25
- [ ] `mimo-music/cmd/server/main.go`：启动 chi HTTP 服务，监听配置端口，优雅关闭
- [ ] `mimo-music/internal/server/router.go`：注册路由
- [ ] `mimo-music/internal/server/handler/health.go`：`GET /health` 返回统一信封
- [ ] `mimo-music/internal/server/response/response.go`：统一信封 `{code, data, message}`
- [ ] `mimo-music/config/config.go`：最小配置（端口、环境），全字段 godoc 注释
- [ ] `mimo-music/.air.toml`：热重载配置
- [ ] `mimo-music/Dockerfile`：多阶段构建
- [ ] `mimo-music/README.md`：运行说明
- [ ] `go build ./cmd/server/` 编译通过
- [ ] 启动后 `curl localhost:8080/health` 返回 `{"code":0,"data":{"status":"ok"},"message":""}`
- [ ] `grep -r "blog-api" mimo-music/` 为空（依赖隔离验证）
- [ ] 所有导出符号有 godoc 注释

## Blocked by

无——可立即开始。这是整个功能的入口切片。
