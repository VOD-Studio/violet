## musicctl completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(musicctl completion bash)

To load completions for every new session, execute once:

#### Linux:

	musicctl completion bash > /etc/bash_completion.d/musicctl

#### macOS:

	musicctl completion bash > $(brew --prefix)/etc/bash_completion.d/musicctl

You will need to start a new shell for this setup to take effect.


```
musicctl completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --json   以 JSON 输出(管道/重定向时自动启用)
      --yes    写操作跳过 y/N 确认(脚本场景)
```

### SEE ALSO

* [musicctl completion](musicctl_completion.md)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 24-Jul-2026
