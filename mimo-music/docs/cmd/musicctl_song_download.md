## musicctl song download

下载歌曲到本地(带元数据)

```
musicctl song download [flags]
```

### Options

```
      --dry-run           只打印将下载到哪、文件名、预估大小,不落盘
      --filename string   文件名模板,支持 {artist}/{title}/{album}/{id}(默认 {artist} - {title})
      --force             覆盖已存在文件
  -h, --help              help for download
      --id int            歌曲 ID
      --level int         音质: 1=standard 2=exhigh 3=lossless 4=hires (default 1)
      --no-metadata       跳过元数据写入(纯流式落盘,更快)
      --out string        下载目录(自动 mkdir -p) (default ".")
```

### Options inherited from parent commands

```
      --json   以 JSON 输出(管道/重定向时自动启用)
      --yes    写操作跳过 y/N 确认(脚本场景)
```

### SEE ALSO

* [musicctl song](musicctl_song.md)	 - 歌曲相关命令

###### Auto generated by spf13/cobra on 24-Jul-2026
